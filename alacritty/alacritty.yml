font:
  normal:
    family: hack
  size: 12

window:
  opacity: 0.9

import:
    - ~/.config/alacritty/themes/dracula.yml

#key_bindings:
  #- { key: T, mods: Control, command: {program: "sh", args: ["-c","nvim ~/.config/alacritty/alacritty.yml"]} }
